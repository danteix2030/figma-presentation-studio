# QA log

2026-09-07 phase 1: pnpm typecheck, test (3 tests), build passed. Figma Desktop registered ID 1678935302970085901. Plugin opened in real PROPOSTA COMERCIAL file and enumerated 172 eligible top-level nodes. Fixed iframe message reception (Figma wrapper does not preserve parent as source). No document uploads performed.

2026-09-08 export repair: identified that the previous build had no export command. Added on-demand Figma PNG rendering plus PDF, PPTX and standalone HTML exporters. TypeScript, 3 tests and production build passed. Installed the build in the registered Figma development plugin. In the real PROPOSTA COMERCIAL file, selected CENA - 01, parsed its layers, rendered the plugin preview, opened Export, generated PDF, and reached the native save dialog with the expected file name and PDF type. PDF/PPTX currently prioritize visual fidelity through raster slide images; editable PowerPoint elements remain pending.

2026-09-08 figs.dec workbench: renamed the development plugin, embedded the supplied logo, rebuilt the interface around slide thumbnails, a central stage, speaker notes, layer tools and a persistent export drawer. Added real Web Animations preview, media/link metadata controls and PNG ZIP export. Build and 3 unit tests passed. Installed and opened in Figma Desktop, loaded all 16 slides from the PPT file, rendered the first slide at correct 16:9 proportions, opened the export drawer, rendered all 16 PDF pages with visible progress and reached the native Windows save dialog.

2026-09-08 ordering/editable export: repaired thumbnail generation by allowing the requested low preview scale. Verified real thumbnails for the deck, per-slide enable/disable, active count, drag handles, up/down actions and S/L/XL resize controls in Figma Desktop. Generated and saved a one-slide editable PPTX; package inspection found 2 editable text nodes, 2 native shapes and 13 separately positioned picture assets. Added SVG transport for eligible vectors and a Google Drive conversion flow that returns the Google Slides edit link. Google upload itself requires an OAuth token and was not executed without user credentials.
